<%= form_with(model: delivery_cost) do |form| %>
  <% if delivery_cost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(delivery_cost.errors.count, "error") %> prohibited this delivery_cost from being saved:</h2>

      <ul>
        <% delivery_cost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :threshold, style: "display: block" %>
    <%= form.text_field :threshold %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
